# 🌐 多域名部署说明

## ✅ 已实现功能

### 动态 URL 显示
页面底部会自动显示当前访问的完整 URL 地址，无论部署在哪个域名下。

### 显示位置
- 位置：页面底部，日历组件下方
- 样式：浅灰色背景框
- 内容：完整的页面 URL

### 自动适配
使用 JavaScript 动态获取当前页面 URL：
```javascript
const currentUrl = window.location.href;
document.getElementById('pageUrl').textContent = currentUrl;
```

## 📱 不同域名下的表现

### 域名 1: 开发服务器
```
URL: https://develop.toppps.com/top-h5/demo/page1/index.html
底部显示: https://develop.toppps.com/top-h5/demo/page1/index.html
```

### 域名 2: GitHub Pages
```
URL: https://viczhao.github.io/web/index.html
底部显示: https://viczhao.github.io/web/index.html
```

### 本地测试
```
URL: http://localhost:8080/index.html
底部显示: http://localhost:8080/index.html
```

## 🔧 技术实现

### HTML 结构
```html
<div class="page-footer">
  <div class="page-url-label">当前页面地址</div>
  <div class="page-url" id="pageUrl">加载中...</div>
</div>
```

### JavaScript 代码
```javascript
// 显示当前页面 URL
const currentUrl = window.location.href;
document.getElementById('pageUrl').textContent = currentUrl;
console.log('🔗 当前页面地址:', currentUrl);
```

### CSS 样式
```css
.page-footer {
  margin-top: auto;
  padding: 20px;
  text-align: center;
  border-top: 1px solid rgba(0, 0, 0, 0.05);
}

.page-url {
  font-size: 12px;
  color: #999;
  word-break: break-all;
  background: #f5f5f5;
  padding: 10px;
  border-radius: 8px;
  margin-top: 10px;
}
```

## 🎯 PWA 安装说明

### PWA 安装与域名无关

重要的是：**PWA 的安装是基于 manifest.json 和 Service Worker 的，与 URL 无关**。

无论部署在哪个域名下，只要满足以下条件即可安装：

1. ✅ HTTPS 环境（或 localhost）
2. ✅ manifest.json 正确配置
3. ✅ Service Worker 成功注册
4. ✅ 图标文件可访问

### 安装后的效果

- **开发服务器安装**：
  - 桌面图标名称：日期倒计时
  - 启动 URL：https://develop.toppps.com/top-h5/demo/page1/index.html

- **GitHub Pages 安装**：
  - 桌面图标名称：日期倒计时
  - 启动 URL：https://viczhao.github.io/web/index.html

两个版本可以**同时安装**在同一设备上，互不干扰！

## 🚀 部署步骤

### 部署到开发服务器
1. 将所有文件上传到：
   ```
   /path/to/top-h5/demo/page1/
   ```

2. 访问：
   ```
   https://develop.toppps.com/top-h5/demo/page1/index.html
   ```

3. 底部会显示对应 URL

### 部署到 GitHub Pages
1. 推送代码到 GitHub 仓库
2. 在仓库设置中启用 GitHub Pages
3. 选择主分支作为源
4. 访问：
   ```
   https://viczhao.github.io/web/index.html
   ```

5. 底部会显示对应 URL

## 📊 对比测试

### 测试清单
- [ ] 开发服务器显示正确 URL
- [ ] GitHub Pages 显示正确 URL
- [ ] 本地测试显示正确 URL
- [ ] PWA 安装功能正常
- [ ] 安装后启动到对应域名

### 验证方法
1. 打开浏览器控制台
2. 查看 `🔗 当前页面地址:` 日志
3. 对比底部显示的 URL
4. 确认两者一致

## 💡 注意事项

### 1. 文件路径
确保所有文件在同一个目录下：
```
page1/
├── index.html
├── manifest.json
├── sw.js
├── icon-192.png
└── icon-512.png
```

### 2. 相对路径
manifest.json 和 sw.js 使用相对路径：
```javascript
// ✅ 正确
href="./manifest.json"
register('./sw.js')

// ❌ 错误
href="/manifest.json"
register('/sw.js')
```

### 3. HTTPS 要求
PWA 必须在 HTTPS 环境下运行（localhost 除外）：
- ✅ `https://develop.toppps.com/...`
- ✅ `https://viczhao.github.io/...`
- ✅ `http://localhost:8080/...`（本地测试）
- ❌ `http://example.com/...`（不支持 PWA）

## 🎉 预期效果

### 页面底部示例
```
┌─────────────────────────────────┐
│                                 │
│         📅 日期倒计时            │
│                                 │
│     [选择日期输入框]             │
│                                 │
│     剩余天数显示区域              │
│                                 │
│     [日历弹出层]                 │
│                                 │
│  ───────────────────────────    │
│                                 │
│      当前页面地址                │
│  ┌─────────────────────────┐   │
│  │ https://develop.toppps  │   │
│  │ .com/top-h5/demo/page1/ │   │
│  │ index.html              │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

### 不同域名下的差异

| 项目 | 开发服务器 | GitHub Pages |
|------|-----------|--------------|
| URL | develop.toppps.com | viczhao.github.io |
| 图标 | 相同 | 相同 |
| 应用名称 | 日期倒计时 | 日期倒计时 |
| 安装后 | 打开开发服务器版本 | 打开 GitHub 版本 |
| 底部显示 | 对应开发服务器 URL | 对应 GitHub URL |

## 🔍 调试技巧

### 查看 URL
打开浏览器控制台，输入：
```javascript
console.log(window.location.href);
```

### 手动测试 URL 显示
在控制台输入：
```javascript
document.getElementById('pageUrl').textContent = '测试 URL';
```

### 清除缓存重新测试
```javascript
localStorage.clear();
location.reload();
```

---

现在您的应用可以灵活部署到任何域名，并且会自动显示当前地址！🚀
